import { AppBar, Toolbar, Typography } from '@material-ui/core'

const Footer = () => {
  return (
    <AppBar position='static' color='primary'>
      <Toolbar>
        <Typography>Board Mamker, created in 2023.</Typography>
      </Toolbar>
    </AppBar>
  )
}

export default Footer
